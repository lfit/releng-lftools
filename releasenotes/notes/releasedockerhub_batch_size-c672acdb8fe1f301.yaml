---
fixes:
  - |
    Dockerhub has implemented a throttling mechanism where after 100-200
    calls to dockerhub, a 6 hour delay is enforced.
    To handle this a batch size of 70 is enforced, when no filter is used.
    Next time, same command is run, next batch is used, and so on.

